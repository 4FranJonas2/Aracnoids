#pragma once
#include "../entities/player.h"

namespace gameMenu
{
	Menu CreateMainMenu(Menu mainMenu);
	Menu CreateCredits(Menu mainMenu);
	Menu CreatePauseMenu(Menu mainMenu);
	Menu CreateWinScreen(Menu mainMenu);
	Menu CreateLoseScreen(Menu mainMenu);
	

}